__pycache__/
*.pyc
*.pyo
*.h5
*.weights.h5
.pytest_cache/
.cache/
simulator/.cache/
*.egg-info/
dist/
build/
.env
*.xlsx
*.csv
training_*/*.h5
training_*/*.weights.h5
Test/training_*/*.h5
Test/training_*/*.weights.h5
Test/episodes_results_*/
Test/checkpoints/
Test/last_state.txt
last_state.txt
batch_*.log
run_parallel.log
run_all.log

# PRIVATE — NEVER track/commit/push (this GitHub repo is PUBLIC). These hold confidential
# meeting/collaborator content + local memory. Do NOT un-ignore or `git add -f` them even if
# asked to "commit everything"; back them up in a PRIVATE repo/gist instead.
.claude/
r5_test_results/memory/
meeting_transcripts/

# LaTeX build intermediates and lock-workaround report duplicates
**/_build/
*_latest.pdf

# Archived local-only training checkpoints (moved from root training_1_*/)
archive/training_runs/

