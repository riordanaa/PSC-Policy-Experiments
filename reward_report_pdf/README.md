# reward_report_pdf — the reward audit (May 2026)

`reward_report.pdf` (+ `.tex` source): audit of the six shaped-reward components in the thesis
DRL pipeline — which emit a usable learning signal as coded, and why several are defective or
saturated. First report of the project; superseded as evidence by the later deterministic studies
(see the root `README.md` study index and `consolidated_report/`). Companion diagnostics live in
the root `r5_test_results*/` folders; follow-up experiments in `reward_fix_report/`.
