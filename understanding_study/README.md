# understanding_study — with routing repaired, what's left? (June 10, 2026)

**Headline (audited):** the allocation channel is worth ≤10–12% (a trivial static rule captures
it; dynamic rules backfire); the remaining residual is mostly dead-factory accounting + transient.
Conclusion: no room left for learned policies in the simple scenario — this set the bar the later
DS-seat and capacity studies built on.

- Report: `report/understanding_report.pdf`. Results notes: `RESULTS.md`.
- Code: `run_allocation_bound.py`, `alloc_policies.py`, `analyze_alloc.py`, `verify_alloc.py`
  (gates), `residual_decomposition.py`, `dynamics_anatomy.py`.
- `results/` is local-only (gitignored).

**Status caveat:** check `consolidated_report/consolidated_findings.pdf` before citing numbers.
